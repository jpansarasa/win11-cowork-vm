# Claude Code local settings (machine-specific)
.claude/

# GitHub personal access tokens — NEVER commit these
.github-pat
.github-pat-bot

# Subagent-driven-development scratch
.superpowers/

# ntfy publish-only token. The real token lives on the HOST at
# /etc/cowork/ntfy.token (root-only) — never in the repo, never in the guest.
ntfy.token
ntfy.json

# Site-specific config overrides (topic URLs, hostnames). Sourced after
# config.env by load_config. Never commit — keeps a public clone leak-free.
config.local.env
